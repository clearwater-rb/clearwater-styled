require "spec_helper"

RSpec.describe Clearwater::Styled do
  it "has a version number" do
    expect(Clearwater::Styled::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
